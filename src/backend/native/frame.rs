use ffi;

use glib::translate::*;

use Frame;

impl Frame {
    pub fn new() -> Frame {
        unimplemented!() // TODO: complete it

        // assert_initialized_main_thread!();
        // unsafe { from_glib_full(ffi::frame_new()) }
    }
}
