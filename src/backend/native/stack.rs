use ffi;

use glib::translate::*;

use Stack;

impl Stack {
    pub fn new() -> Stack {
        unimplemented!() // TODO: complete it

        // assert_initialized_main_thread!();
        // unsafe { from_glib_full(ffi::mx_stack_new()) }
    }
}
