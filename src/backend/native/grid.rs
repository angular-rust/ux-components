use ffi;

use glib::translate::*;

use Grid;

impl Grid {
    pub fn new() -> Grid {
        unimplemented!(); // TODO: complete it

        // assert_initialized_main_thread!();
        // unsafe { from_glib_full(ffi::grid_new()) }
    }
}
