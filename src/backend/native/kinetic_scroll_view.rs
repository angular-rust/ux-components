use ffi;

use glib::translate::*;

use KineticScrollView;

impl KineticScrollView {
    pub fn new() -> KineticScrollView {
        unimplemented!() // TODO: complete it

        // assert_initialized_main_thread!();
        // unsafe { from_glib_full(ffi::mx_kinetic_scroll_view_new()) }
    }
}
