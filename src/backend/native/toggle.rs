use ffi;

use glib::translate::*;

use Toggle;

impl Toggle {
    pub fn new() -> Toggle {
        unimplemented!() // TODO: complete it

        // assert_initialized_main_thread!();
        // unsafe { from_glib_full(ffi::toggle_new()) }
    }
}
