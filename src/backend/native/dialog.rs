use ffi;

use glib::translate::*;

use Dialog;

impl Dialog {
    pub fn new() -> Dialog {
        unimplemented!(); // TODO: complete it

        // assert_initialized_main_thread!();
        // unsafe { from_glib_full(ffi::mx_dialog_new()) }
    }
}
