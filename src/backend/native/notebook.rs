use ffi;

use glib::translate::*;

use Notebook;

impl Notebook {
    pub fn new() -> Notebook {
        unimplemented!() // TODO: complete it

        // assert_initialized_main_thread!();
        // unsafe { from_glib_full(ffi::notebook_new()) }
    }
}
