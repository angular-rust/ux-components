use ffi;

use glib::translate::*;

use Expander;

impl Expander {
    pub fn new() -> Expander {
        unimplemented!(); // TODO: complete it

        // assert_initialized_main_thread!();
        // unsafe { from_glib_full(ffi::expander_new()) }
    }
}
