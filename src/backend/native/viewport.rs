use ffi;

use glib::translate::*;

use Viewport;

impl Viewport {
    pub fn new() -> Viewport {
        unimplemented!() // TODO: complete it

        // assert_initialized_main_thread!();
        // unsafe { from_glib_full(ffi::viewport_new()) }
    }
}
