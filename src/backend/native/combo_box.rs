use ffi;

use glib::translate::*;

use ComboBox;

impl ComboBox {
    pub fn new() -> ComboBox {
        unimplemented!(); // TODO: complete it

        // assert_initialized_main_thread!();
        // unsafe { from_glib_full(ffi::combo_box_new()) }
    }
}
