use ffi;

use glib::translate::*;

use Table;

impl Table {
    pub fn new() -> Table {
        unimplemented!() // TODO: complete it

        // assert_initialized_main_thread!();
        // unsafe { from_glib_full(ffi::mx_table_new()) }
    }
}
