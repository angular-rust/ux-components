use ffi;

use glib::translate::*;

use Button;

impl Button {
    pub fn new() -> Button {
        unimplemented!() // TODO: complete it

        // assert_initialized_main_thread!();
        // unsafe { from_glib_full(ffi::mx_button_new()) }
    }

    pub fn with_label(text: &str) -> Button {
        unimplemented!() // TODO: complete it

        // assert_initialized_main_thread!();
        // unsafe { from_glib_full(ffi::mx_button_new_with_label()) }
    }
}
