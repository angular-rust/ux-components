use ffi;

use glib::translate::*;

use Image;

impl Image {
    pub fn new() -> Image {
        unimplemented!() // TODO: complete it

        // assert_initialized_main_thread!();
        // unsafe { from_glib_full(ffi::mx_image_new()) }
    }
}
