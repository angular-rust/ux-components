use ffi;

use glib::translate::*;

use Icon;

impl Icon {
    pub fn new() -> Icon {
        unimplemented!() // TODO: complete it

        // assert_initialized_main_thread!();
        // unsafe { from_glib_full(ffi::icon_new()) }
    }
}
