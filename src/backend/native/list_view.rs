use ffi;

use glib::translate::*;

use ListView;

impl ListView {
    pub fn new() -> ListView {
        unimplemented!() // TODO: complete it

        // assert_initialized_main_thread!();
        // unsafe { from_glib_full(ffi::list_view_new()) }
    }
}
