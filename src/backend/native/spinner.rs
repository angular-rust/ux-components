use ffi;

use glib::translate::*;

use Spinner;

impl Spinner {
    pub fn new() -> Spinner {
        unimplemented!() // TODO: complete it

        // assert_initialized_main_thread!();
        // unsafe { from_glib_full(ffi::spinner_new()) }
    }
}
