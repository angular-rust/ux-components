use ffi;

use glib::translate::*;

use FadeEffect;

impl FadeEffect {
    pub fn new() -> FadeEffect {
        unimplemented!(); // TODO: complete it

        // assert_initialized_main_thread!();
        // unsafe { from_glib_full(ffi::mx_fade_effect_new()) }
    }
}
