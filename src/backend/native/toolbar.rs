use ffi;

use glib::translate::*;

use Toolbar;

impl Toolbar {
    pub fn new() -> Toolbar {
        unimplemented!() // TODO: complete it

        // assert_initialized_main_thread!();
        // unsafe { from_glib_full(ffi::mx_toolbar_new()) }
    }
}
