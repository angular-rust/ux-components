use ffi;

use glib::translate::*;

use PathBar;

impl PathBar {
    pub fn new() -> PathBar {
        unimplemented!() // TODO: complete it

        // assert_initialized_main_thread!();
        // unsafe { from_glib_full(ffi::path_bar_new()) }
    }
}
